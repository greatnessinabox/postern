{
  "name": "@postern/protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Protocol adapters for IMAP, JMAP, Gmail REST, and MS Graph.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@postern/core": "workspace:^",
    "imapflow": "^1.3.3",
    "mailparser": "^3.9.8"
  },
  "devDependencies": {
    "@postern/ai": "workspace:*",
    "@postern/tsconfig": "workspace:^",
    "@types/mailparser": "^3.4.6"
  }
}
