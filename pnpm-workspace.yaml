packages:
  - 'apps/*'
  - 'packages/*'
  - 'tools/*'

# pnpm 11 blocks dependency build scripts by default. esbuild and sharp ship
# native binaries and their install scripts are safe and expected.
allowBuilds:
  esbuild: true
  sharp: true
