packages:
  - 'apps/*'
  - 'packages/*'
  - 'tools/*'

# pnpm 11 blocks dependency build scripts by default. esbuild ships its
# native binary and its install script is safe and expected.
allowBuilds:
  esbuild: true
